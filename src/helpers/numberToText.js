const numberToText = (value) => {
  if (value === 0) {
    return 'one'
  }
  if (value === 1) {
    return 'two'
  }
  if (value === 2) {
    return 'three'
  }
  if (value === 3) {
    return 'four'
  }
}

export default numberToText
